import SwiftUI

extension Font {
    /// Applies the bundled Balatro-inspired display font at a fixed point size.
    static func balatroChrome(_ size: CGFloat) -> Font {
        .custom("m6x11plus", size: size)
    }
}
